extends ./layout.jade

block content
    main.flex-shrink-0(role='main')
        .container
            h1.mt-5= title
            h2(class=classes)= message
            p.lead&attributes(atts)
                | Take Off!
                | A fixed navbar has been added with 
                code padding-top: 60px;
                |  on the
                code main > .container
                | 
            p
                | Back to
                a(href='../examples/sticky-footer/') the default sticky footer
                | 					minus the navbar.
            ul 
                each user in users
                    li id: #{user.id}: name: #{user.name}, email: #{user.email}, #{user.job}

block content
